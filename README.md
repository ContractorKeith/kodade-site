# kodade-site

Static landing page for ködade, the Agentic Development Environment.

## Local preview

Open `index.html` directly in a browser. The page has no build step and no runtime
dependencies.

## Project notes

- Brand and product direction lives in `DESIGN.md`.
- The design exploration source is kept in `Ködade Brand.dc.html`.
- Keep the shipped page static and lightweight unless the product site needs a
  real download or account flow later.
